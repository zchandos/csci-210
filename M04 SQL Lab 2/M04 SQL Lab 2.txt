--#1 SELECT EMP_FNAME, EMP_LNAME, JOB_DESCRIPTION FROM EMPLOYEE INNER JOIN JOB ON EMPLOYEE.JOB_CODE = JOB.JOB_CODE
--#2 SELECT ASSIGN_NUM, ASSIGN_DATE, ASSIGN_JOB, ASSIGN_CHG_HR, ASSIGN_HOURS, ASSIGN_CHARGE FROM ASSIGNMENT, PROJECT WHERE ASSIGNMENT.PROJ_NUM = PROJECT.PROJ_NUM and PROJ_NAME = 'Starflight'
--#3 SELECT EMP_FNAME, EMP_LNAME, EMP_HIREDATE FROM EMPLOYEE , JOB WHERE EMPLOYEE.JOB_CODE = JOB.JOB_CODE and JOB_DESCRIPTION = 'DSS Analyst'
--#4 SELECT count(EMP_NUM) FROM PROJECT
--#5 SELECT EMP_FNAME, EMP_LNAME, sum(ASSIGN_CHARGE) FROM EMPLOYEE JOIN ASSIGNMENT ON EMPLOYEE.EMP_NUM = ASSIGNMENT.EMP_NUM GROUP BY EMPLOYEE.EMP_FNAME, EMPLOYEE.EMP_FNAME
--#6 SELECT * FROM EMPLOYEE JOIN JOB ON EMPLOYEE.JOB_CODE = JOB.JOB_CODE JOIN PROJECT ON EMPLOYEE.EMP_NUM = PROJECT.EMP_NUM WHERE EMPLOYEE.JOB_CODE = 500 and PROJECT.PROJ_NAME = 'Amber Wave'
--#7 SELECT PROJ_NAME, PROJ_VALUE, sum(ASSIGN_HOURS) FROM PROJECT join ASSIGNMENT ON PROJECT.PROJ_NUM = ASSIGNMENT.PROJ_NUM GROUP BY PROJECT.PROJ_NAME, PROJECT.PROJ_VALUE
